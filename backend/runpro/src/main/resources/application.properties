spring.application.name=runpro
spring.profiles.active=dev

# ===== JPA (common) =====
spring.jpa.open-in-view=false

# ===== JWT =====
app.jwt.expiration-hours=${APP_JWT_EXPIRATION_HOURS:24}

# ===== Actuator =====
management.endpoints.web.exposure.include=health
